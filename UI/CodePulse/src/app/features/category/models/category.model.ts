export interface AddCategoryRequest{
    name : string;
    urlHandle : string;
}

//You can create another interface also
export interface Category{
    id: string;
     name : string;
    urlHandle : string;
}